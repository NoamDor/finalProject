@model finalProject.Models.Supplier

<div class='page-title-container'>
    <h2 class='page-title'>מחיקת ספק</h2>
</div>

<div class='form-container'>
    <h3>האם אתה בטוח שברצונך למחוק ספק זה?</h3>
    @using (Html.BeginForm("Delete", "Suppliers", FormMethod.Post, new { @class = "form" }))
    {
        @Html.AntiForgeryToken()
        <img style="height: 10vh;width: 6vw; margin-bottom: 5%;"
             src=@System.String.Format("/Images/Suppliers/{0}", Model.PictureName) />
        <div>
            @Html.LabelFor(model => model.Name, new { @class = "form-label" })
            @Html.DisplayFor(model => model.Name)
        </div>
        <div class='form-group form-footer'>
            <input type="hidden" name="id" value="@Model.Id" />
            <input type="submit" value="מחק" class="submit-form-button btn btn-danger" />
            @Html.ActionLink("לרשימה המלאה", "Index", "Suppliers", null, new { @class = "full-list-action" })
        </div>
    }
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
